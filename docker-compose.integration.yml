services:
  postgres-test:
    image: postgres:16
    container_name: ludo-postgres-test
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: ludo_test
    ports:
      - "5433:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d ludo_test"]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 5s
